// import { Calendar } from '../src/calendar';

describe('Calendar', () => {
	describe('constructor', () => {
		it('initializes with default values', () => {
			// const calendar = new Calendar({view: 'week'});
			// console.log('Calendar view: ', calendar.view);
			// console.log('Calendar date target: ', calendar.target);
			// console.log('Calendar data: ', calendar.data);
			// calendar.data.cells.flat().forEach(item => {
			//   console.log(item);
			// });
			expect(true).toBe(true);
		});
	});
});
